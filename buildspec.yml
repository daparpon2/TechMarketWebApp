version: 0.2
phases:
  build:
      commands:
      - echo "Entered the build phase"
      - mvn clean install
  post_build:
      commands:
      - echo "Entered the post-build phase"
      - mv target/*.war /.
artifacts:
    files:
        - target/**/*